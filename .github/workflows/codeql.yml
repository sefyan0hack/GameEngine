name: "CodeQL Advanced"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '24 1 * * 2'

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: windows-latest
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
          - language: c-cpp
            build-mode: manual

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: ${{ matrix.language }}
        build-mode: ${{ matrix.build-mode }}

    - name: Set up CMake
      uses: cmake/setup-cmake@v3
      with:
        cmake-version: '3.25.0'  # Specify the CMake version you need

    - name: Build C++ Project with CMake
      run: |
        echo "Running CMake build for C++ project"
        mkdir build
        cd build
        cmake ..  # You can add more options here, such as -DCMAKE_BUILD_TYPE=Release
        cmake --build . --config Release  # Modify this if you're using a different configuration or build type

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: "/language:${{matrix.language}}"
